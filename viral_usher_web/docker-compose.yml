version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - viral_usher_data:/data
    environment:
      - WORKDIR=/data
    restart: unless-stopped

volumes:
  viral_usher_data:
